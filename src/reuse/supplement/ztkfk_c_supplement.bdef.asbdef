projection implementation in class ZTKFK_bp_c_supplement unique;
strict;
use draft;

define behavior for ZTKFK_C_Supplement alias Supplement
{
  use create ( augment );
  use update ( augment );
  use delete;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;

  field ( modify ) SupplementDescription;
}